'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  contentContainer: {
    width: '100%',
    flexDirection: 'column',
    alignItems: 'center',
  },
  buttonText: {
    color: '#000000',
  },
});
